class Account:
    def __init__(self, account_id, balance, number_card, pin_card):
        self.account_id = account_id
        self.balance = balance
        self.number_card = number_card
        self.pin_card = pin_card
    
